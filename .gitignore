# ⚠️ 密钥文件,绝对不能提交(里面是真实 API 钥匙)
.env

# Python 虚拟环境和缓存(换机器重装即可,不用带走)
venv/
__pycache__/
*.pyc

# 运行时产生的文件
server.log
pending_actions.json
# 定时任务运行时状态,跟机器走,不进版本库
# (注意:.gitignore 不支持"行尾注释",# 只有写在行首才算注释)
scheduled_tasks.json

# 账号数据(密码哈希 + 每个账号的聊天记录),绝不能进 git
data/
